source "https://rubygems.org"

gem "fastlane"
gem "cocoapods"
# representable >= 3.2 requires multi_json at runtime but no longer declares it
# as a dependency; fastlane loads it via create_app_on_managed_play_store.
gem "multi_json"
