abstract final class AppRoutes {
  /// The app shell hosting the top-level bottom-navigation destinations.
  /// Also the launch route and the target after a full-stack reset.
  static const shell = '/';
}
