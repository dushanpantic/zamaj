name: Release • TestFlight • Internal Testing

on:
  workflow_dispatch:
    inputs:
      runner:
        description: 'Runner to execute on (must be macOS-capable)'
        type: choice
        required: true
        default: self-hosted
        options:
          - self-hosted
          - macos-latest
      release_notes:
        description: 'Release notes shown to testers (optional)'
        type: string
        required: false
        default: ''

permissions:
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/_release_ios.yml
    with:
      lane: internal
      runner: ${{ inputs.runner }}
      release_notes: ${{ inputs.release_notes }}
    secrets: inherit
